import { NavLink } from "react-router-dom"


export const Nav = () => {
    return (
        <nav>
            <ul>
                <NavLink to="/">Home</NavLink>
            </ul>
        </nav>
    )
}